# 避免 AI 幻觉的分析方法总结

这份方法来自 `misc2.md` 中的解题复盘。它的核心思想是：不要让“看起来很可疑”的东西直接变成结论，而是让每一步都有证据、验证和反证。

---

## 1. 先提取题面中的明确线索

不要一上来就猜隐藏点。先把题面里真正出现过的关键词列出来，并判断它们可能指向什么技术方向。

例如原文中：

- “回响”提示 echo / echo hiding
- “重复播放的音乐”提示音频信号
- “镜厅”“镜面”“图像映射”可能是视频方向，但未必是真载体

方法：

1. 把题面关键词拆出来。
2. 给每个关键词列出可能技术含义。
3. 优先验证和题面关键词强相关的方向。
4. 不把装饰性描述直接当成技术事实。

---

## 2. 区分“异常现象”和“有效证据”

异常不等于有 flag。原文中 2.7 MB 的 H.264 SEI 数据非常反常，但经过熵、magic bytes、加密尝试和结构分析后，没有形成可解证据链，所以应当降级为诱饵或未证实线索。

判断标准：

- 是否能提取出稳定结构？
- 是否能被多个工具或脚本复现？
- 是否有可解释的编码、文件头、明文特征或统计特征？
- 是否能推进到下一步，而不是只停留在“很可疑”？

如果答案是否定的，就不要写成确定结论。

推荐表述：

> 这里存在异常，但目前没有证据证明它是有效载体。

避免表述：

> 这里一定藏了 flag。

---

## 3. 每个结论都要配一个验证动作

防幻觉最有效的方法是把推理变成可复现操作。

示例：

- 怀疑 ZIP 伪加密：对比 Local File Header 和 Central Directory 的 GPB flag。
- 怀疑音频藏 echo：用倒谱分析寻找延迟峰。
- 怀疑 Morse：用完整 Morse 表逐组解码。
- 怀疑视频帧 LSB：统计比特翻转率，而不是凭肉眼判断。

写分析时可以采用这个格式：

```text
假设：信息藏在音频回声里。
验证：提取 wav，做倒谱分析。
结果：在 100 和 130 samples 处有稳定峰值。
结论：存在双延迟 echo hiding 的强证据。
```

这样可以避免 AI 把“猜测”包装成“事实”。

---

## 4. 用反证清理红鲱鱼

原文中视频方向做了很多尝试，最后证明大多是 red herring。重要的是：不要只记录成功路径，也要记录为什么某些路径被排除。

可以用表格整理：

| 怀疑点 | 验证方法 | 结果 | 是否继续 |
| --- | --- | --- | --- |
| 视频藏字 | 透视矫正、边缘检测 | 无稳定文字结构 | 放弃 |
| 帧间 LSB | 统计翻转率 | 接近压缩噪声 | 放弃 |
| SEI 数据 | 熵分析、magic bytes、常见加密尝试 | 无可解结构 | 降级为诱饵 |
| 音频回声 | 倒谱分析 | 出现稳定双延迟峰 | 继续 |

这种写法能防止后续分析被早期直觉绑架。

---

## 5. 只在数据支持范围内下结论

AI 容易把缺失部分补完。解决办法是明确区分三类内容：

- 已验证事实
- 合理推测
- 未证实猜想

示例：

```text
已验证事实：倒谱在 100 和 130 samples 处有峰。
合理推测：这是双延迟 echo hiding，分别表示 bit 0 和 bit 1。
未证实猜想：SEI 尾部数据可能是诱饵。
```

写 writeup 时，不要把“未证实猜想”写成“出题人一定如何如何”。

---

## 6. 参数选择要说明来源

原文中窗口长度 `2205 samples` 不是凭空来的，而是扫描得到的：这个窗口下前 74 字节可打印，并且置信度最高。

防幻觉写法：

```text
窗口长度不是手动拍脑袋确定的，而是在 1000 到 8000 samples 范围内扫描，比较可打印 ASCII 比例和 peak 差值后选出的。
```

参数需要交代：

- 扫描范围
- 判断指标
- 最终选择原因
- 是否存在边界或低置信度位置

---

## 7. 输出结果前检查编码表和格式假设

原文中 Morse 解码必须使用带标点的 ITU-R M.1677 表。如果只用字母数字表，会误以为 `?`、`/`、`(`、`+` 是错误结果。

检查项：

- 当前编码表是否完整？
- 是否包含标点、大小写、空格或特殊字符？
- flag 格式是题目规定，还是自己假设？
- 解出的字符是否能由原始数据逐步复现？

推荐写法：

```text
这些标点不是异常字符，而是完整 Morse 标点表中的合法结果。
```

---

## 8. 面向 AI 的防幻觉提示词模板

让 AI 辅助分析时，可以直接使用下面的约束：

```text
请按“假设 - 验证 - 结果 - 结论”的格式分析。
不要把猜测写成事实。
所有结论必须说明来自哪一步数据或脚本输出。
如果某个现象只是异常但不能继续解码，请标记为“未证实”或“可能是诱饵”。
参数选择必须说明依据，例如扫描范围、判定指标和置信度。
最后请列出仍然不确定的地方。
```

---

## 9. 最终方法论

一句话总结：

> 先从题面线索建立假设，再用可复现脚本验证；异常只能作为线索，不能直接作为结论；每个关键判断都要留下数据证据和反证记录。

适合在 CTF、取证、隐写、逆向和 AI 辅助写 writeup 时使用。

